- name: user exists or not
  hosts: localhost
  tasks:
  - name: user exists or not
    ansible.builtin.command: id roboshop
    register: output
  - name: print output
    ansible.builtin.debug:
     msg: "{{output}}"
