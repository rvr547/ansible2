- name: preceedence of variable
  hosts: test
  vars:
    Name: abc play
  
  vars_files:
    var.yaml
  
  vars_prompt:
  - name: Name
    prompt: Enter Name
  
  tasks:
  - name: print task var
    vars: 
    Name: task var
    ansible.builtin.debug:
      msg: " Iam printing {{Name}}"
